<Page
    x:Class="Lab2UwpApp.BlankPage1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Lab2UwpApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="CornflowerBlue">
        <Button Content="Get Menu Document" Background="Aquamarine" Margin="54,67,0,0" VerticalAlignment="Top" Height="196" Width="221" Click="Button_Click" FontSize="20" FontWeight="Bold"/>
        <StackPanel Margin="475,67,102,52" Background="#FFF3F3F3">
            <GridView x:Name="dtCustomer">
                <GridView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            
                            <StackPanel>
                                <TextBlock Text="{Binding Image}" Margin="5"/>
                                <TextBlock Text="{Binding Name}" Margin="5"/>
                                <TextBlock Text="{Binding Price}" Margin="5"/>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
        </StackPanel>


    </Grid>
</Page>
