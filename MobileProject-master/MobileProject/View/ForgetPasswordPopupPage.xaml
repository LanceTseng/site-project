<?xml version="1.0" encoding="utf-8" ?>
<rg:PopupPage
    x:Class="MobileProject.View.ForgetPasswordPopupPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:rg="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup">
    <StackLayout
        BackgroundColor="Transparent"
        HorizontalOptions="Center"
        VerticalOptions="Center">
        <Frame
            Padding="20"
            BackgroundColor="DarkBlue"
            CornerRadius="10"
            HasShadow="True"
            HeightRequest="400"
            WidthRequest="300">
            <StackLayout
                Padding="20"
                BackgroundColor="AliceBlue"
                Spacing="15">
                <Label FontSize="Medium" Text="Enter your new password:" />

                <!--  Password Entry with Masking  -->
                <Entry
                    x:Name="PasswordEntry"
                    IsPassword="True"
                    Placeholder="New Password"
                    Text="{Binding NewPassword, Mode=TwoWay}" />

                <StackLayout HorizontalOptions="Center" Orientation="Horizontal">
                    <Button Command="{Binding CancelCommand}" Text="Cancel" />
                    <Button Command="{Binding ProcessResetPasswordCommand}" Text="Reset" />
                </StackLayout>
            </StackLayout>
        </Frame>
    </StackLayout>
</rg:PopupPage>